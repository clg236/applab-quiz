import React from 'react';
import QuizSubmissionDetail from './QuizSubmissionDetail';

const SubmissionDetail = props => (
    <QuizSubmissionDetail isAssignment {...props}/>
);

export default SubmissionDetail;